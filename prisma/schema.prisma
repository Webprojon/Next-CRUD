generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Tasks {
  id          String  @id @default(cuid())
  member      String
  task        String
  priority    String
  iscompleted Boolean
}
